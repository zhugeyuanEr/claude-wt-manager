---
description: 列出所有 worktree
---
# Worktree 列表

显示所有 worktree 的列表信息。

## 使用方式

```
/wt-list
```

## 输出内容

每个 worktree 显示:
- 路径
- 分支名称
- 当前所在 worktree 标记 (HEAD)
- 关联信息

## 执行命令

运行 `git worktree list` 获取所有 worktree 信息。
